<h1>Categories Index</h1>
<% @categories.each do |category| %>
  <h2><%= link_to "#{category.name}", category_path(category) %></h2>
<% end %> 
<%= link_to 'add category', new_category_path, class: "btn btn-success btn-xs" %>

require 'spec_helper'

describe "prizes/new.html.erb" do

  before(:each) do
    render
  end

  # it "should show prize form" do
  #   expect(rendered).to have_selector('form#new_prize') 
  # end

  # it "should show prize element name field" do
  #   expect(rendered).to have_field("Name")
  # end

  # it "should show prize element value field" do
  #   expect(rendered).to have_field("Value")
  # end

  # it "should have submit button" do
  #   expect(rendered).to have_selector('input[type=submit]')
  # end

end
